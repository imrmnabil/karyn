{
  "name": "kryn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "npm run build --prefix frontend && npm start --prefix backend",
    "db:seed:images": "npm run db:seed:images --prefix backend"
  }
}
